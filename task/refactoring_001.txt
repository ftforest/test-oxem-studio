Спасибо, комментарии по решению:
1. Стоит придерживаться принципа 1 файл = 1 класс и использовать автозагрузку классов по стандартам PSR.
2. Метод называется removeProduct, но в описании к нему речь не про продукт, а про животное.
3. Определения методов дублируются в классах животных, стоило бы вынести их в класс-родитель.
4. Если хранить животных в классе self::$animals не общим массивом, а отдельными массивами для каждого типа животных, то можно упростить метод getCountAnimalsType, считая количество просто методом count
5. Почему выбрана реализация через статические методы?